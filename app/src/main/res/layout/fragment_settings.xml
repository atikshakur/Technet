<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/settings_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/background"
        android:elevation="4dp"
        app:titleTextAppearance="@style/TextAppearance.Widget.AppCompat.Toolbar.Subtitle"
        app:titleTextColor="@color/purple_text" />

    <TextView
        android:layout_below="@+id/settings_toolbar"
        android:layout_marginTop="6dp"
        android:id="@+id/support_settings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:text="@string/support"
        android:textColor="@color/purple_text" />

    <ListView
        android:layout_below="@id/support_settings"
        android:id="@+id/settings_support_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_marginTop="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:id="@+id/about_settings"
        android:layout_below="@id/settings_support_listview"
        android:text="@string/about"
        android:textColor="@color/purple_text"
        android:layout_marginStart="15dp" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/about_listview_settings"
        android:layout_below="@id/about_settings"
        android:layout_marginTop="8dp" />


</RelativeLayout>